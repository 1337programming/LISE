import { CoreObject } from '@Engine/CoreObject/CoreObject';

/**
 * @module CoreObject
 * A StaticMesh is a piece of geometry that consists of a static set of polygons. Static Meshes can be
 * translated, rotated, and scaled, but they cannot have their vertices animated in any way. As such, they are more
 * efficient to render than other types of geometry such as SkeletalMesh , and they are often the basic building
 * block of levels created in the engine.
 */
export class StaticMesh extends CoreObject {
  
}
